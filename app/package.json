{
  "name": "reelforge-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute reelforge-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute reelforge-db --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "wrangler": "^4.114.0",
    "@cloudflare/workers-types": "^5.20260831.0",
    "typescript": "^5.6.0"
  }
}
